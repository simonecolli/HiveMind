/**
 * Shared props for every lucide icon in the interface.
 *
 * The stroke is lighter than the library default: this UI is built on 1px
 * rules and small type, and a 2px glyph next to it reads as a different weight.
 */
export const ICON = { size: 15, strokeWidth: 1.75 } as const;
